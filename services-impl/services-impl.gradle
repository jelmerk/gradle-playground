apply plugin: 'java'
apply plugin: 'maven'

archivesBaseName = 'playground-services-impl'
description='The implementation of the services'

dependencies {
    compile project(':services-api')
    compile libraries.httpclient
    testCompile libraries.junit
}