apply plugin: 'war'
apply plugin: 'jetty'
apply plugin: 'maven'

archivesBaseName = 'playground-web'
description='The web layer'

dependencies {
    compile(project(':services-api'))
    compile(project(':services-impl'))
}

httpPort = 8080
stopPort = 9451
stopKey = 'foo'